{
  "name": "autest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch packages/daemon/src/index.ts",
    "daemon": "node --env-file=.env --import=tsx packages/daemon/src/index.ts",
    "web": "pnpm --filter @autest/web dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
